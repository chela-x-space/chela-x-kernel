[package]
name = "kernel-studio"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
kernel-domain = { path = "../kernel-domain" }
kernel-gateway = { path = "../kernel-gateway" }
